apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: {{ template "api.fullname" . }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ template "api.name" . }}
    chart: {{ template "api.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ template "api.name" . }}
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ template "api.name" . }}
        release: {{ .Release.Name }}
    spec:
      containers:
      - name: {{ .Values.Name }}
        image: {{ .Values.Image }}
        imagePullPolicy: {{ .Values.PullPolicy }}
        ports:
        - containerPort: {{ .Values.ContainerPort }}
        volumeMounts:
        - mountPath: /tmp/logs
          name: {{ .Values.Name }}-vol
        env:
        - name: PROFILE
          value: {{ .Values.global.Profile }}
        - name: bootstrap-servers
          value: "{{ .Values.global.hostIP }}:{{ .Values.global.kafkaPort }}"
        - name: EUREKA_URL
          value: "{{ .Values.global.EUREKA_URL }}"
        - name: LOG_PATH
          value: "{{ .Values.global.LOG_PATH }}"
        - name: ZIPKIN_URL
          value: "{{ .Values.global.ZIPKIN_URL }}"
      restartPolicy: Always
      volumes:
      - name: {{ .Values.Name }}-vol
        hostPath:
          path: "{{ .Values.global.LOG_PATH }}"