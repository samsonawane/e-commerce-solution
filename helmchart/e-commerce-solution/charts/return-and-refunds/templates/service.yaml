apiVersion: v1
kind: Service
metadata:
  name: {{ template "statusupdate.fullname" . }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ template "statusupdate.name" . }}
    chart: {{ template "statusupdate.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.type }}
  ports:
  - name: {{ .Values.Name }}-port
    port: {{ .Values.ContainerPort }}
    protocol: TCP
    targetPort: {{ .Values.ContainerPort }}
    nodePort: {{ .Values.Port }}
  selector:
    app: {{ template "statusupdate.name" . }}
    release: {{ .Release.Name }}
