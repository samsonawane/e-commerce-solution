apiVersion: v1
kind: Service
metadata:
  name: {{ template "query.fullname" . }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ template "query.name" . }}
    chart: {{ template "query.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.type }}
  ports:
  - name: {{ .Values.Name }}-port
    port: {{ .Values.ContainerPort }}
    protocol: TCP
    targetPort: {{ .Values.ContainerPort }}
    nodePort: {{ .Values.Port }}
  selector:
    app: {{ template "query.name" . }}
    release: {{ .Release.Name }}
